# Notes Review Log

更新时间: 2026-02-26

## 本轮已完成
- 审阅并统一 `00-24` 的文档骨架，新增“本章定位 / 设计 Why / 阅读建议（进阶）”。
- 批量移除路径后面的行号锚点，统一为“文件路径 + 类/函数”引用方式。
- 每篇补充“与其他章节关系 / 最小可验证实验 / 常见误解”。
- 修复已识别的章节编号冲突（如编译缓存小节编号）。

## 当前结论
- 文档更适合进阶读者做系统化深挖，跨版本追溯稳定性提升。
- 仍可继续做二次精修：减少跨文档重复解释、补更强的性能对照模板。

## 深度事实审校进展（第 1 轮）
- 修正 `09-attention-backends.md` 标题编号错误（08 -> 09）。
- 将高频源码锚点从裸文件名统一到可定位路径（例如 `scheduler.py` -> `python/sglang/srt/managers/scheduler.py`）。
- 清理残留的“文件名 + Lxxx”引用形式，减少跨版本漂移。
- 对 `03-scheduler.md` 中 `schedule_enhancer` 段落完成版本核对：当前代码未检索到对应文件/调用，已改为“历史机制待确认”说明。

## 深度事实审校进展（第 2 轮）
- 完成源码引用一致性修复：未解析反引号源码引用从 `218` 降至 `0`（以仓库实际文件存在性校验）。
- 修正多处高风险路径歧义（如 `scheduler.py`、`model_runner.py`、`server_args.py` 等统一到 SRT 主实现路径）。
- 修正多模态模型文件表中的路径和命名错误（含 `NanoNemotronVL` 命名与文件路径）。
- 修正/澄清历史内容：`schedule_enhancer` 在当前 v0.5.9 代码中未检索到实现，已显式标记为历史机制待确认。

## 深度事实审校进展（第 3 轮：符号级语义）
- 对 `03/08/14` 三篇执行符号级核对（函数名、方法名、关键调用链）。
- 核验结果：除已标记为历史机制的 `schedule_enhancer` 外，核心符号均可在当前代码中定位。
- 修正 1 处章节错链：`03-scheduler.md` 中 `§22.1` 更正为 `§20.1`。
- 复核结果：当前 `00-24` 文档中的反引号源码路径可解析项保持 `0` 未解析。

## 深度事实审校进展（第 4 轮：符号级语义）
- 对 `09/10/11` 三篇执行符号级核对（函数名、方法名、关键入口）。
- 修正 `10-model-loading.md` 中不存在的符号：`per_channel_quant_fp8()` -> `per_token_group_quant_fp8()` / `sgl_per_tensor_quant_fp8()`。
- 修正 `11-multimodal.md` 请求入口符号：`v1_chat_completions()` -> `openai_v1_chat_completions()`，并将入口文件改为 `python/sglang/srt/entrypoints/http_server.py`。
- 复核结果：`09/10/11` 的反引号函数符号当前均可在代码中定位（missing=0）。
